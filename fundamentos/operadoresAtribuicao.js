const a = 7 // => estamos atribuindo um valor 
let b = 3
b += a // => atribuição aditiva acrescentando o valor de a na variavel b ==( b = b + a)
console.log(b)
//O mesmo ocorre com a subtração
b -= 4 // => atribuição subtrativa subtraindo o valor 4 de b == (b = b-4)
console.log(b)

b *= 2 // Atribuição multiplicativa estou atribuindo o valor de b*2 a b ou b = 6 * 2 = 12 que pode ser codado assim => (b = b * 2)
console.log(b)

b /= 2 // b = b /2  pode ser assim tambem

b %= 2 // b = b % 2  ou b = (6/2=3) que sobra (0) para valores pares se fosse impar iria sobrar 1
console.log(b)
